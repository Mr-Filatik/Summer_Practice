using AuktionEPAM.ENTITY;
using Interfaces;
using System;
using System.Collections.Generic;
using System.Text;

namespace AuktionEPAM.BLL
{
    /*public class UserManager : Interfaces.IUserManager
    {
        public static IUserManager iUserStorage;
        //private static int id_user = 1;

        public int GetLog(string log)
        {
            iUserStorage.GetLog(log);
        }
        public int GetPas(string pas)
        {
            iUserStorage.GetPas(pas);
        }
        public User GetUser(int id)
        {
            return iUserStorage.GetUser(id);
        }

        public UserManager()
        {
            iUserStorage = new UserManager();
        }
    }*/
}
